/* Consiste no içamento (hoisting) das variáveis,
 * que são movidas sempre para o topo do código,
 * o seu valor, porém, permanece na mesma linha
 * da declaração.
 */

console.log(nome);
var nome = 'William';
// Retorna undefined