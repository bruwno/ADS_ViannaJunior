// O que irá aparecer no console?
if (('Gato' === 'gato') && (5 > 2)) {
    console.log('Verdadeiro');
} else {
    console.log('Falso');
}
/* Falso, uma vez que apesar de 5 > 2 ser true, Gato === gato é false, 
 * já que o operador === exige que os dados comparados sejam exatamente iguais.
*/